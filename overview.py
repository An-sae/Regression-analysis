# Rename this file to  secrets.toml  to switch on password protection.
# Without a secrets.toml file the app runs with no password (fine locally).
#
# Choose a password and write it below. Everyone who should reach the app
# needs it. Change it when staff leave.

password = "BYT-MIG-2026"
